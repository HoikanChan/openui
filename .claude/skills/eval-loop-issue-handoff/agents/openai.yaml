interface:
  display_name: "Eval-Loop Issue Handoff"
  short_description: "Turn eval findings into evidence-rich tracker issues with snapshots, screenshots, and data models."
  default_prompt: "Use $eval-loop-issue-handoff to turn this eval run into high-leverage tracker issues with concrete evidence and acceptance criteria."
