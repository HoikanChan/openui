# Interception corpus — prompt view dump

Generated by `InterceptionCorpusDumpTest`; build artifact, do not commit.
Issues show the FULL validator output; the prompt section shows what survives
cascade suppression in `ReaskPromptBuilder`.

---

## valid-table-basic (valid-control, valid)

### DSL

```
root = Stack([header, breakdownTable])
header = TextContent("Regional Sales Breakdown", "large")
breakdownTable = Table([regionCol, revenueCol, updatedCol], data.report.breakdown)
regionCol = Col("Region", "region")
revenueCol = Col("Revenue", "revenue", {cell: @Render("v", TextContent(@FormatNumber(v, 0, "en-US") + " USD"))})
updatedCol = Col("Updated", "updatedAt", {cell: @Render("v", TextContent(@FormatDate(v, "dateTime")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-table-render-cell (valid-control, valid)

### DSL

```
root = Stack([ordersTable])
ordersTable = Table([idCol, statusCol], data.orders)
idCol = Col("Order ID", "id")
statusCol = Col("Status", "status", {cell: @Render("v", "row", TextContent(row.id + ": " + @Switch(v, {"paid": "Paid", "pending": "Pending", "mystery": "Mystery"}, "Unknown")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-table-sortable-date (valid-control, valid)

### DSL

```
root = Stack([employeeTable])
employeeTable = Table([nameCol, salaryCol, joinedCol], data.employees)
nameCol = Col("Name", "name")
salaryCol = Col("Salary", "salary", {sortable: true, cell: @Render("v", TextContent(@FormatNumber(v, 0, "en-US") + " USD"))})
joinedCol = Col("Joined", "joinedAt", {sortable: true, cell: @Render("v", TextContent(@FormatDate(v, "date")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-list-action-items (valid-control, valid)

### DSL

```
root = Stack([actionItemsList])
actionItemsList = List(data.list.items, data.list.title)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- List(items: any[], title?: string)
```

---

## valid-card-kpi (valid-control, valid)

### DSL

```
root = Stack([performanceCard])
performanceCard = Card([cardHeader, cardContent], "card", "standard")
cardHeader = CardHeader("Q1 Performance")
cardContent = Stack([TextContent("This is the Q1 performance summary for the quarter.", "default")], "column", "m", "start", "start")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Card(children: any[], variant?: string, size?: string)
- CardHeader(title: string)
- TextContent(text: string, variant?: string)
```

---

## valid-bar-product-comparison (valid-control, valid)

### DSL

```
root = Stack([chartTitle, revenueChart])
chartTitle = TextContent("Quarterly Revenue by Product Line", "large")
labels = data.labels
series = data.series
productAValues = @Each(series, "s", s.category == "Product A" ? s.values : null)
productBValues = @Each(series, "s", s.category == "Product B" ? s.values : null)
productASeries = Series("Product A", productAValues)
productBSeries = Series("Product B", productBValues)
revenueChart = BarChart(labels, [productASeries, productBSeries], "grouped", "Quarter", "Revenue ($)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-pie-sales-by-region (valid-control, valid)

### DSL

```
root = Stack([chartTitle, pieChart])
chartTitle = TextContent("Sales Distribution by Region", "large")
pieChart = PieChart(data.labels, data.values, "pie")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- PieChart(labels: any[], values: any[], variant?: string)
```

---

## valid-line-monthly-revenue (valid-control, valid)

### DSL

```
root = Stack([chartTitle, revenueChart])
chartTitle = TextContent("Monthly Revenue Trend", "large")
labels = data.labels
seriesData = @Each(data.series, "item", item.values)
revenueSeries = Series("Revenue", seriesData)
revenueChart = LineChart(labels, [revenueSeries], "smooth", "Month", "Revenue (USD)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-scope-syslog-steps (valid-scope-control, valid)

### DSL

```
root = Stack([header, logSteps])
header = CardHeader("Syslog Records", "Total: " + data.syslogTotal)
logSteps = Steps(@Each(data.syslogResult, "log", logStepTpl))
logStepTpl = StepsItem(log.syslogBrief + " (" + log.neName + ")", logCard)
logCard = Card([timeRow, detailRow], "column", "s")
timeRow = Stack([timeLabel, timeValue], "row", "none", "center")
timeLabel = TextContent("Time: ", "small")
timeValue = TextContent(@FormatDate(log.time, "dateTime"), "small")
detailRow = TextContent(log.syslogDetail, "small")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Steps(items: any[])
- StepsItem(title: string, details?: any)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-online-device-info (valid-scope-control, valid)

### DSL

```
root = Stack([header, tableCard], "column", "m")
header = CardHeader("在线设备信息", "共 " + data.deviceTotal + " 台设备")
tableCard = Card([deviceTable])
deviceTable = Table([nameCol, ipCol, typeCol, vendorCol, roleCol, versionCol, categoryCol, fabricCol], data.deviceInfo)
nameCol = Col("设备名称", "neName")
ipCol = Col("IP 地址", "neIp")
typeCol = Col("设备型号", "neType")
vendorCol = Col("厂商", "neVendor")
roleCol = Col("角色", "neRole")
versionCol = Col("版本", "neVersion")
categoryCol = Col("类别", "neCategory")
fabricCol = Col("Fabric", "fabricName")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-grouped-model-tabs (valid-scope-control, valid)

### DSL

```
root = Stack([summaryCard, modelTabs], "column", "m")
summaryCard = Card([summaryHeader, summaryContent], "card", "column", "m", "start", "start", false)
summaryHeader = CardHeader("Online Devices Summary", "Fabric: " + data.fabric)
summaryContent = Stack([totalText, fabricText], "row", "m", "center", "start", false)
totalText = TextContent("Total Online Devices: " + data.total_count, "large-heavy")
fabricText = TextContent("Fabric: " + data.fabric, "default")
modelTabs = Tabs(@Each(data.grouped_by_model, "group", modelTabTpl))
modelTabTpl = TabItem(group.model, group.model + " (" + group.count + " devices)", [modelTable])
modelTable = Table([modelCol, countCol, versionCol, nameCol, ipCol, vendorCol], group.devices)
modelCol = Col("Model", "model", {cell: @Render("v", "row", TextContent(row.model ?? group.model))})
countCol = Col("Count", "count", {cell: @Render("v", "row", TextContent("" + (row.count ?? group.count)))})
versionCol = Col("Version", "version")
nameCol = Col("Device Name", "name")
ipCol = Col("IP", "ip")
vendorCol = Col("Vendor", "vendor")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Tabs(items: any[])
- TabItem(value: string, label: string, content: any[])
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-peer-interface-info (valid-scope-control, valid)

### DSL

```
root = Stack([infoCard])
infoCard = Card([header, details])
header = CardHeader("Peer Interface Information", "Fabric: " + (data.peer_interface_info["Fabric"] ?? "N/A"))
details = Descriptions([
  DescField("Local Device", data.peer_interface_info["本端设备"] ?? "No data"),
  DescField("Local Device IP", data.peer_interface_info["本端设备IP"] ?? "No data"),
  DescField("Local Interface", data.peer_interface_info["本端接口"] ?? "No data"),
  DescField("Remote Device", data.peer_interface_info["对端设备"] ?? "No data"),
  DescField("Remote Device IP", data.peer_interface_info["对端设备IP"] ?? "No data"),
  DescField("Remote Interface", data.peer_interface_info["对端接口"] ?? "No data"),
  DescField("Remote IP Address", data.peer_interface_info["对端IP地址"] ?? "No data"),
  DescField("Management Status", data.peer_interface_info["对端管理状态"] ?? "No data"),
  DescField("Operational Status", data.peer_interface_info["对端操作状态"] ?? "No data")
], "Connection Details")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Descriptions(items: any[], title?: string, columns?: number, border?: boolean)
- DescField(label: string, value: any, span?: number)
```

---

## valid-scope-interface-bandwidth-tabs (valid-scope-control, valid)

### DSL

```
root = Stack([header, tabs])
header = CardHeader("Interface Bandwidth Metrics", "Top interfaces by input and output byte rate")
inputGroup = data.queryInterfaceMetrics[0]
outputGroup = data.queryInterfaceMetrics[1]
inputData = inputGroup.telemetryData
outputData = outputGroup.telemetryData
tabs = Tabs([inputTab, outputTab])
inputTab = TabItem("input", "Input Bandwidth", [inputCard])
outputTab = TabItem("output", "Output Bandwidth", [outputCard])
inputCard = Card([inputHeader, inputTable])
inputHeader = CardHeader("Top Input Interfaces", "Byte rate (B/s)")
inputTable = Table([inputNameCol, inputRateCol, inputDeviceCol], inputData)
inputNameCol = Col("Interface", "ifName")
inputRateCol = Col("Byte Rate", "input_byte_rate", {cell: @Render("v", TextContent(@FormatBytes(v)))})
inputDeviceCol = Col("Device", "neName")
outputCard = Card([outputHeader, outputTable])
outputHeader = CardHeader("Top Output Interfaces", "Byte rate (B/s)")
outputTable = Table([outputNameCol, outputRateCol, outputDeviceCol], outputData)
outputNameCol = Col("Interface", "ifName")
outputRateCol = Col("Byte Rate", "output_byte_rate", {cell: @Render("v", TextContent(@FormatBytes(v)))})
outputDeviceCol = Col("Device", "neName")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Tabs(items: any[])
- TabItem(value: string, label: string, content: any[])
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-bd-interface-mapping (valid-scope-control, valid)

### DSL

```
root = Stack([header, bdTableCard], "column", "m")

header = CardHeader("BD Interface Mapping", "Device: " + data.device + " (" + data.deviceIp + ")")

bdTableCard = Card([bdTable])

bdTable = Table([bdIdCol, subinterfacesCol], data.data)

bdIdCol = Col("BD ID", "bdId")

subinterfacesCol = Col("Sub-interfaces", "subinterfaces", {cell: @Render("v", v.length > 0 ? TagBlock(v) : TextContent("None"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- TagBlock(tags: string[])
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-top-cpu-table (valid-scope-control, valid)

### DSL

```
root = Stack([header, tableCard])
header = CardHeader(data.title, "Top 5 devices by CPU usage")
sortedData = @Sort(data.data, "cpu_usage", "desc")
tableCard = Card([deviceTable])
deviceTable = Table([nameCol, ipCol, macCol, cpuCol], sortedData)
nameCol = Col("设备名称", "neName")
ipCol = Col("设备IP", "neIp")
macCol = Col("MAC地址", "mac")
cpuCol = Col("CPU利用率(%)", "cpu_usage", {cell: @Render("v", TextContent(@FormatPercent(v / 100, 2)))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-device-cpu-kpis (valid-scope-control, valid)

### DSL

```
root = Stack([header, kpiRow, deviceTableCard], "column", "m")

header = CardHeader("Device CPU Usage", "Top " + @Count(data.devices) + " devices by CPU load")

kpiRow = Stack([totalCard, avgCard, maxCard], "row", "m", "stretch", "start", true)
totalCard = Card([TextContent("Total Devices", "small"), TextContent("" + data.total_devices, "large-heavy")])
avgCard = Card([TextContent("Avg CPU Usage", "small"), TextContent(@FormatPercent(@Avg(data.devices.cpu_usage), 1), "large-heavy")])
maxCard = Card([TextContent("Max CPU Usage", "small"), TextContent(@FormatPercent(@Max(data.devices.cpu_usage), 1), "large-heavy")])

deviceTableCard = Card([tableHeader, deviceTable])
tableHeader = CardHeader("Device Details", "Ranked by CPU usage")
deviceTable = Table([rankCol, nameCol, ipCol, cpuCol], data.devices)

rankCol = Col("Rank", "rank")
nameCol = Col("Device Name", "neName")
ipCol = Col("IP Address", "neIp")
cpuCol = Col("CPU Usage", "cpu_usage", {cell: @Render("v", TextContent(@FormatPercent(v, 1)))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-device-cpu-detail (valid-scope-control, valid)

### DSL

```
root = Stack([deviceCard, detailsCard], "column", "m")

deviceCard = Card([deviceHeader, cpuTag], "column", "m", "center", "center")
deviceHeader = TextContent("CPU Usage", "large-heavy")
cpuTag = TextContent(data.cpu_usage_percent + "%", "large-heavy")

detailsCard = Card([detailsDesc], "column", "none")
detailsDesc = Descriptions([ipField, deviceField, macField, timeField, aggField], "Device Details", 1, true)
ipField = DescField("IP Address", data.ip)
deviceField = DescField("Device", data.device)
macField = DescField("MAC Address", data.mac)
timeField = DescField("Time Range", data.time_range)
aggField = DescField("Aggregation", data.aggregation)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Descriptions(items: any[], title?: string, columns?: number, border?: boolean)
- DescField(label: string, value: any, span?: number)
```

---

## valid-scope-port-summary-table (valid-scope-control, valid)

### DSL

```
root = Stack([summaryHeader, kpiRow, deviceTableCard], "column", "m")

summaryHeader = CardHeader("Network Device Port Summary", "Overview of 4 devices and 228 total ports")

kpiRow = Stack([totalDevicesKpi, totalPortsKpi, activeOperKpi, activeAdminKpi], "row", "m", "stretch", "start", true)

totalDevicesKpi = Card([TextContent("Total Devices", "small"), TextContent("" + data.summary.total_devices, "large-heavy")])
totalPortsKpi = Card([TextContent("Total Ports", "small"), TextContent("" + data.summary.total_ports, "large-heavy")])
activeOperKpi = Card([TextContent("Operationally Active", "small"), TextContent("" + data.summary.operStatus_dist.active, "large-heavy")])
activeAdminKpi = Card([TextContent("Administratively Active", "small"), TextContent("" + data.summary.adminStatus_dist.active, "large-heavy")])

deviceTableCard = Card([deviceTableHeader, deviceTable])
deviceTableHeader = CardHeader("Device Details", "Port status breakdown per device")

deviceTable = Table([nameCol, ipCol, totalPortsCol, adminActiveCol, adminInactiveCol, operActiveCol, operInactiveCol], data.devices)

nameCol = Col("Device Name", "deviceName", {cell: @Render("v", TextContent(v, "default"))})
ipCol = Col("IP Address", "neIp", {cell: @Render("v", TextContent(v, "default"))})
totalPortsCol = Col("Total Ports", "totalPorts", {cell: @Render("v", TextContent("" + v, "default"))})
adminActiveCol = Col("Admin Active", "adminStatus", {cell: @Render("v", TextContent("" + (v.active ?? 0), "default"))})
adminInactiveCol = Col("Admin Inactive", "adminStatus", {cell: @Render("v", TextContent("" + (v.inactive ?? 0), "default"))})
operActiveCol = Col("Oper Active", "operStatus", {cell: @Render("v", TextContent("" + (v.active ?? 0), "default"))})
operInactiveCol = Col("Oper Inactive", "operStatus", {cell: @Render("v", TextContent("" + (v.inactive ?? 0), "default"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-nested-device-version-cards (valid-scope-control, valid)

### DSL

```
root = Stack([header, summaryCards], "column", "m")
header = Card([titleText, subtitleText])
titleText = TextContent(data.title, "large-heavy")
subtitleText = TextContent(data.subtitle, "small")
summaryCards = Stack(@Each(data.summary, "item", deviceCardTpl), "column", "m")
deviceCardTpl = Card([deviceHeader, versionList], "column", "s")
deviceHeader = Stack([neTypeTag, totalCountTag], "row", "s", "center")
neTypeTag = Tag(item.neType, "info", null, "md")
totalCountTag = Tag("共 " + item.totalCount + " 台", "neutral", null, "md")
versionList = Stack(@Each(item.versionList, "ver", versionRowTpl), "column", "s")
versionRowTpl = Card([versionInfo, deviceNamesList], "column", "xs", "start", "start", false)
versionInfo = Stack([versionTag, countTag], "row", "s", "center")
versionTag = Tag(ver.version, "neutral", null, "sm")
countTag = Tag(ver.deviceCount + " 台", "success", null, "sm")
deviceNamesList = Stack(@Each(ver.deviceNames, "name", nameTagTpl), "row", "xs", "start", "start", true)
nameTagTpl = Tag(name, "neutral", null, "sm")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-device-type-tabs-render-each (valid-scope-control, valid)

### DSL

```
root = Stack([titleCard, summaryRow, deviceTypeTabs], "column", "m")

titleCard = Card([titleHeader], "card")
titleHeader = CardHeader(data.title, "设备总数: " + data.summary.totalDevices + " · 型号数: " + data.summary.totalModels)

summaryRow = Stack([totalDevicesCard, totalModelsCard, inconsistentCard], "row", "m", "stretch", "start", true)
totalDevicesCard = Card([TextContent("设备总数", "small"), TextContent("" + data.summary.totalDevices, "large-heavy")])
totalModelsCard = Card([TextContent("设备型号", "small"), TextContent("" + data.summary.totalModels, "large-heavy")])
inconsistentCard = Card([TextContent("版本不一致型号", "small"), TextContent("" + data.summary.versionInconsistentModels, "large-heavy")])

deviceTypeTabs = Tabs(@Each(data.deviceTypeStats, "type", typeTabTpl))
typeTabTpl = TabItem(type.neType, type.neType + " (" + type.totalCount + ")", [typeCard])
typeCard = Card([typeHeader, versionTable], "column", "m")
typeHeader = CardHeader(type.neType, "共 " + type.totalCount + " 台设备")
versionTable = Table([versionCol, countCol, devicesCol], type.versionBreakdown)
versionCol = Col("版本", "version")
countCol = Col("数量", "count")
devicesCol = Col("设备列表", "devices", {cell: @Render("v", deviceListTpl)})
deviceListTpl = Stack(@Each(v, "dev", devTagTpl), "row", "xs", "start", "start", true)
devTagTpl = Tag(dev, "neutral", null, "sm")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Tabs(items: any[])
- TabItem(value: string, label: string, content: any[])
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-nested-device-version-compact (valid-scope-control, valid)

### DSL

```
root = Stack([header, summaryCards], "column", "m")

header = Card([titleText, subtitleText])
titleText = TextContent(data.title, "large-heavy")
subtitleText = TextContent(data.subtitle, "small")

summaryCards = Stack(@Each(data.summary, "item", deviceCardTpl), "column", "m")

deviceCardTpl = Card([deviceHeader, versionList], "column", "s")
deviceHeader = Stack([neTypeTag, totalCountTag], "row", "s")
neTypeTag = Tag(item.neType, "info", "md")
totalCountTag = Tag("共 " + item.totalCount + " 台", "neutral", "md")

versionList = Stack(@Each(item.versionList, "ver", versionRowTpl), "column", "s")

versionRowTpl = Card([versionInfo, deviceNamesList], "column", "xs", "start", "start", false)
versionInfo = Stack([versionText, countText], "row", "s", "center")
versionText = TextContent("版本: " + ver.version, "default")
countText = Tag(ver.deviceCount + " 台", "neutral", "sm")

deviceNamesList = Stack(@Each(ver.deviceNames, "name", nameTagTpl), "row", "xs", "start", "start", true)
nameTagTpl = Tag(name, "neutral", "sm")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-link-optical-tabs (valid-scope-control, valid)

### DSL

```
root = Stack([header, kpiRow, tabs], "column", "m")
header = CardHeader(data.deviceName + " (" + data.deviceIp + ")", "Fabric: " + data.fabricName)
kpiRow = Stack([linkKpi, opticalKpi], "row", "m", "stretch", "start", false)
linkKpi = Card([TextContent("Total Links", "small"), TextContent("" + data.linkCount, "large-heavy")])
opticalKpi = Card([TextContent("Optical Modules", "small"), TextContent("" + data.opticalCount, "large-heavy")])
tabs = Tabs([linksTab, modulesTab])
linksTab = TabItem("links", "Links (" + data.linkCount + ")", [linksCard])
modulesTab = TabItem("modules", "Optical Modules (" + data.opticalCount + ")", [modulesCard])
linksCard = Card([CardHeader("Link Details", "Active connections"), linksTable])
linksTable = Table([linkNameCol, aPortCol, zPortCol, aNeCol, zNeCol, rateCol, typeCol, statusCol], data.links)
linkNameCol = Col("Link Name", "linkName")
aPortCol = Col("A-Port", "aPortName")
zPortCol = Col("Z-Port", "zPortName")
aNeCol = Col("A-NE", "aNeName")
zNeCol = Col("Z-NE", "zNeName")
rateCol = Col("Rate", "layerRate")
typeCol = Col("Type", "linkType")
statusCol = Col("Status", "linkStatus", {cell: @Render("v", v == "在线" ? Tag("Online", "success") : Tag("Offline", "danger"))})
modulesCard = Card([CardHeader("Optical Module Inventory", "Transceiver details"), modulesTable])
modulesTable = Table([snCol, vendorCol, typeCol2, ifNameCol, adminCol, operCol, certCol], data.opticalModules)
snCol = Col("SN", "opticalSn")
vendorCol = Col("Vendor", "opticalVendor")
typeCol2 = Col("Transceiver Type", "transType")
ifNameCol = Col("Interface", "ifName")
adminCol = Col("Admin Status", "adminStatus", {cell: @Render("v", v == "Up" ? Tag("Up", "success") : Tag("Down", "danger"))})
operCol = Col("Oper Status", "operStatus", {cell: @Render("v", v == "Up" ? Tag("Up", "success") : Tag("Down", "danger"))})
certCol = Col("Certification", "huaweiCertified", {cell: @Render("v", v == "已认证" ? Tag("Certified", "success") : Tag("Uncertified", "warning"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Tabs(items: any[])
- TabItem(value: string, label: string, content: any[])
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-cpu-data-availability (valid-scope-control, valid)

### DSL

```
root = Stack([titleCard, summaryRow, detailsCard, noDataCard], "column", "m")

titleCard = Card([titleHeader], "card")
titleHeader = CardHeader(data.title, "查询时间范围: " + data.summary.queryTimeRange)

summaryRow = Stack([totalCard, withDataCard, avgCpuCard], "row", "m", "stretch")
totalCard = Card([TextContent("设备总数", "small"), TextContent("" + data.summary.deviceTotal, "large-heavy")])
withDataCard = Card([TextContent("有数据设备", "small"), TextContent("" + data.summary.deviceWithData, "large-heavy")])
avgCpuCard = Card([TextContent("平均CPU利用率", "small"), TextContent(data.summary.avgCpuUsage, "large-heavy")])

detailsCard = Card([detailsHeader, detailsTable], "card")
detailsHeader = CardHeader("设备详情", "有CPU指标数据的设备列表")
detailsTable = Table([nameCol, ipCol, fabricCol, cpuCol], data.deviceDetails)
nameCol = Col("设备名称", "neName")
ipCol = Col("IP地址", "neIp")
fabricCol = Col("Fabric", "fabricName")
cpuCol = Col("CPU利用率", "cpuUsage", {cell: @Render("v", TextContent(v, "default"))})

noDataCard = Card([noDataHeader, noDataTable], "card")
noDataHeader = CardHeader("无数据设备", "未获取到CPU指标数据的设备")
noDataTable = Table([noNameCol, noIpCol, noFabricCol, reasonCol], data.deviceWithoutData)
noNameCol = Col("设备名称", "neName")
noIpCol = Col("IP地址", "neIp")
noFabricCol = Col("Fabric", "fabricName")
reasonCol = Col("原因", "reason", {cell: @Render("v", Tag(v, "warning"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-device-monitoring-logs (valid-scope-control, valid)

### DSL

```
root = Stack([headerCard, metricsRow, logsCard], "column", "m")

headerCard = Card([headerTitle, headerInfo], "column", "none")
headerTitle = TextContent("设备监控概览", "large-heavy")
headerInfo = Stack([deviceNameText, deviceIpText], "row", "s")
deviceNameText = TextContent("设备: " + data.memorySection.data.deviceName, "small")
deviceIpText = TextContent("IP: " + data.memorySection.data.deviceIP, "small")

metricsRow = Stack([cpuCard, memoryCard], "row", "m", "stretch")

cpuCard = Card([cpuHeader, cpuValue], "column", "s")
cpuHeader = TextContent(data.cpuSection.title, "small")
cpuValue = TextContent(@FormatPercent(data.cpuSection.data.cpuUsageAvg / 100, 2), "large-heavy")

memoryCard = Card([memHeader, memValue], "column", "s")
memHeader = TextContent(data.memorySection.title, "small")
memValue = TextContent(@FormatPercent(data.memorySection.data.memUsageAvg / 100, 2), "large-heavy")

logsCard = Card([logsHeader, logsTable], "column", "m")
logsHeader = CardHeader(data.logsSection.title, "时间范围: " + data.logsSection.timeRange)
logsTable = Table([timeCol, severityCol, briefCol, detailCol], data.logsSection.data)

timeCol = Col("时间", "time")
severityCol = Col("级别", "severity", {cell: @Render("v", severityCellTpl)})
briefCol = Col("摘要", "brief")
detailCol = Col("详情", "detail")

severityCellTpl = @Switch(v, {"Notice": Tag("Notice", "info"), "Warning": Tag("Warning", "warning")}, Tag(v, "neutral"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- CardHeader(title?: string, subtitle?: string)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-fabric-device-inventory (valid-scope-control, valid)

### DSL

```
root = Stack([header, summaryRow, deviceTableCard], "column", "m")

header = CardHeader("Fabric Device Inventory", "Scene: " + data.deployScene)

summaryRow = Stack([totalCard, onlineCard], "row", "m", "stretch", "start")
totalCard = Card([TextContent("Total Devices", "small"), TextContent("" + data.deviceTotal, "large-heavy")])
onlineCount = @Count(@Filter(data.deviceInfo, "neState", "==", "online"))
onlineCard = Card([TextContent("Online", "small"), TextContent("" + onlineCount, "large-heavy")])

deviceTableCard = Card([deviceTable])
deviceTable = Table([nameCol, ipCol, typeCol, roleCol, versionCol, stateCol], data.deviceInfo)

nameCol = Col("Name", "neName")
ipCol = Col("IP", "neIp")
typeCol = Col("Type", "neType")
roleCol = Col("Role", "neRole")
versionCol = Col("Version", "neVersion")
stateCol = Col("State", "neState", {cell: @Render("v", stateCellTpl)})

stateCellTpl = @Switch(v, {"online": Tag("Online", "success")}, Tag("Offline", "danger"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-no-data-device-list (valid-scope-control, valid)

### DSL

```
root = Stack([header, top3Card, noDataCard], "column", "m")

header = CardHeader("Server Leaf CPU Analysis", "Top 3 highest CPU usage devices and devices with no data")

top3Card = Card([top3Header, top3Table])
top3Header = CardHeader("Top 3 Server Leaf CPU Usage", "Devices with highest CPU utilization")
top3Table = Table([rankCol, nameCol, ipCol, cpuCol, fabricCol], data.top3_serverleaf_cpu)

rankCol = Col("Rank", "rank")
nameCol = Col("Device Name", "neName")
ipCol = Col("IP Address", "neIp")
cpuCol = Col("CPU Usage (%)", "cpu_usage", {cell: @Render("v", Tag(v, v >= 80 ? "danger" : v >= 60 ? "warning" : "info"))})
fabricCol = Col("Fabric", "fabricName")

noDataCard = Card([noDataHeader, noDataList])
noDataHeader = CardHeader("Devices With No Data", "Devices that did not report data")
noDataList = Stack(@Each(data.no_data_devices, "device", deviceTagTpl), "column", "none")
deviceTagTpl = Tag(device, "neutral")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-client-experience-trend (valid-scope-control, valid)

### DSL

```
root = Stack([trendCard])
trendCard = Card([trendHeader, trendChart])
trendHeader = CardHeader("Client Experience Trend", "Value over time")
chartData = data.data[0]
timestampLabels = @Each(chartData, "item", @FormatDate(item.timestamp, "dateTime"))
sampleValues = @Each(chartData, "item", item.value)
trendChart = LineChart(timestampLabels, [Series("Value", sampleValues)], "smooth", "Time", "%")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- LineChart(labels: string[], series: any[], variant?: "linear" | "smooth" | "step", xLabel?: string, yLabel?: string)
- Series(category: string, values: number[])
```

---

## valid-scope-fabric-type-tabs (valid-scope-control, valid)

### DSL

```
root = Stack([fabricTabs])
fabricTabs = Tabs(@Each(data.data, "fabric", fabricTabTpl))
fabricTabTpl = TabItem(fabric.fabricName, fabric.fabricName, [fabricCard])
fabricCard = Card([fabricHeader, typeTable])
fabricHeader = CardHeader("Device Types in " + fabric.fabricName, "Total Types: " + fabric.totalTypes)
typeTable = Table([typeCol, countCol], fabric.types)
typeCol = Col("Type", "type")
countCol = Col("Count", "count")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Tabs(items: any[])
- TabItem(value: string, label: string, content: any[])
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-fault-location-details (valid-scope-control, valid)

### DSL

```
root = Stack([detailCard])
detailCard = Card([header, details])
header = CardHeader("故障定位详情", data.issueLocationPathName)
details = Descriptions([
  DescField("问题类型", data.problemType),
  DescField("告警名称", data.alarmName),
  DescField("是否告警", data.isAlarm),
  DescField("根因类型", data.alarmRootType),
  DescField("业务层级", data.trailLevel),
  DescField("业务名称", data.trailName),
  DescField("光纤名称", data.fiberName),
  DescField("故障定位网元", data.issueLocationNeName),
  DescField("故障定位路径", data.issueLocationPathName),
  DescField("诊断信息", data.diagnoseInfo)
], "详细信息")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Descriptions(items: any[], title?: string, columns?: number, border?: boolean)
- DescField(label: string, value: any, span?: number)
```

---

## valid-scope-fib-usage-table (valid-scope-control, valid)

### DSL

```
root = Stack([header, tableCard], "column", "m")
header = CardHeader("设备板卡资源使用率", "FIB表项利用率监控")
tableCard = Card([table])
table = Table([slotCol, deviceCol, ipCol, macCol, fibv4Col, fibv6Col], data.list)
slotCol = Col("槽位名称", "slot_name")
deviceCol = Col("设备名称", "device_name")
ipCol = Col("设备IP", "device_ip")
macCol = Col("MAC地址", "mac")
fibv4Col = Col("FIBv4利用率", "fibv4_usage", {cell: @Render("v", TextContent(@FormatPercent(v / 100, 1)))})
fibv6Col = Col("FIBv6利用率", "fibv6_usage", {cell: @Render("v", TextContent(@FormatPercent(v / 100, 1)))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-ospf-neighbors (valid-scope-control, valid)

### DSL

```
root = Stack([localCard, neighborCard], "column", "m")

localCard = Card([localHeader, localDetails], "card")
localHeader = CardHeader("Local Interface", data.localInterface.deviceName)
localDetails = Descriptions([
  DescField("Device IP", data.localInterface.deviceIp),
  DescField("Interface", data.localInterface.interfaceName)
])

neighborCard = Card([neighborHeader, neighborTable], "card")
neighborHeader = CardHeader("OSPF Neighbors", "Count: " + @Count(data.ospfNeighbors))
neighborTable = Table([
  Col("Device Name", "deviceName"),
  Col("Device IP", "deviceIp"),
  Col("Interface", "interfaceName")
], data.ospfNeighbors)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Descriptions(items: any[], title?: string, columns?: number, border?: boolean)
- DescField(label: string, value: any, span?: number)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## valid-scope-congestion-diagnosis (valid-scope-control, valid)

### DSL

```
root = Stack([reportHeader, phenomenonCard, rootCauseCard, bottleneckCard, repairCard], "column", "l")

reportHeader = CardHeader("网络拥塞诊断报告", "故障等级: " + data.congestion_diagnosis.fault_level)

phenomenonCard = Card([phenomHeader, phenomDesc], "card", "column", "m")
phenomHeader = CardHeader("故障现象", "")
phenomDesc = Descriptions([
  DescField("故障时间", data.congestion_diagnosis.fault_phenomenon.fault_time),
  DescField("拥塞设备", data.congestion_diagnosis.fault_phenomenon.congestion_device),
  DescField("拥塞接口", data.congestion_diagnosis.fault_phenomenon.congestion_interface),
  DescField("拥塞时长", data.congestion_diagnosis.fault_phenomenon.congestion_duration),
  DescField("平均带宽利用率", data.congestion_diagnosis.fault_phenomenon.bandwidth_utilization.average),
  DescField("峰值带宽利用率", data.congestion_diagnosis.fault_phenomenon.bandwidth_utilization.peak),
  DescField("峰值时间", data.congestion_diagnosis.fault_phenomenon.bandwidth_utilization.peak_time),
  DescField("丢包率", data.congestion_diagnosis.fault_phenomenon.loss_rate),
  DescField("最大时延", data.congestion_diagnosis.fault_phenomenon.max_delay),
  DescField("影响范围", data.congestion_diagnosis.fault_phenomenon.impact_scope)
], "故障详情", 2, true)

rootCauseCard = Card([rcHeader, rcContent], "card", "column", "m")
rcHeader = CardHeader("根因分析", "置信度: " + data.congestion_diagnosis.root_cause.confidence)
rcContent = Stack([rcType, rcDesc, rcEvidence], "column", "m")
rcType = TextContent("类型: " + data.congestion_diagnosis.root_cause.type, "default")
rcDesc = TextContent(data.congestion_diagnosis.root_cause.description, "default")
rcEvidence = Stack([evidenceHeader, evidenceList], "column", "s")
evidenceHeader = TextContent("证据链:", "small-heavy")
evidenceList = Stack(@Each(data.congestion_diagnosis.root_cause.evidence, "item", evidenceItemTpl), "column", "s")
evidenceItemTpl = TextContent(item, "small")

bottleneckCard = Card([bnHeader, bnDesc], "card", "column", "m")
bnHeader = CardHeader("瓶颈分析", "")
bnDesc = Descriptions([
  DescField("设备", data.congestion_diagnosis.bottleneck_analysis.device),
  DescField("入方向接口", data.congestion_diagnosis.bottleneck_analysis.in_interface),
  DescField("出方向接口", data.congestion_diagnosis.bottleneck_analysis.out_interface),
  DescField("带宽比例", data.congestion_diagnosis.bottleneck_analysis.bandwidth_ratio),
  DescField("拥塞类型", data.congestion_diagnosis.bottleneck_analysis.congestion_type),
  DescField("受影响流", data.congestion_diagnosis.bottleneck_analysis.affected_flows)
], "瓶颈详情", 2, true)

repairCard = Card([repairHeader, repairContent], "card", "column", "m")
repairHeader = CardHeader("修复建议", "")
repairContent = Stack([primaryActionCard, tempActionsCard, longTermActionsCard], "column", "m")

primaryActionCard = Card([paHeader, paDesc], "card", "column", "s")
paHeader = CardHeader("主要行动", "紧急程度: " + data.repair_recommendations.primary_action.urgency)
paDesc = Descriptions([
  DescField("行动", data.repair_recommendations.primary_action.action),
  DescField("目标", data.repair_recommendations.primary_action.target),
  DescField("描述", data.repair_recommendations.primary_action.description),
  DescField("预期效果", data.repair_recommendations.primary_action.expected_effect)
], "", 1, false)

tempActionsCard = Card([taHeader, taList], "card", "column", "s")
taHeader = CardHeader("临时措施", "")
taList = Stack(@Each(data.repair_recommendations.temporary_actions, "item", taItemTpl), "column", "m")
taItemTpl = Card([taAction, taTarget, taDesc, taUrgency], "card", "column", "none")
taAction = TextContent("行动: " + item.action, "small-heavy")
taTarget = TextContent("目标: " + item.target, "small")
taDesc = TextContent(item.description, "small")
taUrgency = TextContent("紧急程度: " + item.urgency, "small")

longTermActionsCard = Card([ltaHeader, ltaList], "card", "column", "s")
ltaHeader = CardHeader("长期措施", "")
ltaList = Stack(@Each(data.repair_recommendations.long_term_actions, "item", ltaItemTpl), "column", "m")
ltaItemTpl = Card([ltaAction, ltaDesc, ltaUrgency], "card", "column", "none")
ltaAction = TextContent("行动: " + item.action, "small-heavy")
ltaDesc = TextContent(item.description, "small")
ltaUrgency = TextContent("紧急程度: " + item.urgency, "small")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Descriptions(items: any[], title?: string, columns?: number, border?: boolean)
- DescField(label: string, value: any, span?: number)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
```

---

## valid-scope-interface-status-table (valid-scope-control, valid)

### DSL

```
root = Stack([summaryRow, tableCard], "column", "m")

summaryRow = Stack([totalCard, activeCard, inactiveCard], "row", "m", "stretch")
totalCard = Card([TextContent("Total Interfaces", "small"), TextContent("" + @Count(data.aiInstanceData), "large-heavy")])
activeCard = Card([TextContent("Active", "small"), TextContent("" + @Count(@Filter(data.aiInstanceData, "adminStatus", "==", "active")), "large-heavy")])
inactiveCard = Card([TextContent("Inactive", "small"), TextContent("" + @Count(@Filter(data.aiInstanceData, "adminStatus", "==", "inactive")), "large-heavy")])

tableCard = Card([tableHeader, interfaceTable])
tableHeader = CardHeader("Interface Details", "List of network interfaces and their status")

interfaceTable = Table([nameCol, deviceCol, roleCol, adminCol, operCol, speedCol, macCol, fabricCol], data.aiInstanceData)

nameCol = Col("Interface Name", "ifName")
deviceCol = Col("Device", "neName", {cell: @Render("v", "row", TextContent(v + " (" + row.neIp + ")"))})
roleCol = Col("Role", "neRole")
adminCol = Col("Admin Status", "adminStatus", {cell: @Render("v", v == "active" ? Tag("Active", "success") : Tag("Inactive", "danger"))})
operCol = Col("Oper Status", "operStatus", {cell: @Render("v", v == "active" ? Tag("Active", "success") : Tag("Inactive", "danger"))})
speedCol = Col("Speed", "ifHighSpeed", {cell: @Render("v", v == "0" ? TextContent("--") : TextContent(v + " Mbps"))})
macCol = Col("MAC Address", "ifPhysAddress")
fabricCol = Col("Fabric", "fabricName")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- TextContent(text: any, size?: "small" | "default" | "large" | "small-heavy" | "large-heavy")
- CardHeader(title?: string, subtitle?: string)
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
- Tag(text: any, variant?: "neutral" | "info" | "success" | "warning" | "danger", icon?: string, size?: "sm" | "md" | "lg")
```

---

## valid-scope-wireless-health-dimensions (valid-scope-control, valid)

### DSL

```
root = Stack([reportHeader, dimCards])
reportHeader = CardHeader("无线健康度报告", "综合健康分: " + data.healthScore)
dimCards = Stack(@Each(data.data, "dim", dimTpl))
dimTpl = Card([CardHeader(dim.key, "得分: " + dim.value.rootValue + " · 排名: " + dim.value.rank), LineChart(@Each(dim.value.baseline, "b", @FormatDate(b.timestamp, "date")), [Series("Baseline", dim.value.baseline.value), Series("Essential", dim.value.essential.value)], "smooth", "时间", "数值"), Stack([Table([Col("区域", "regionName"), Col("数值", "value"), Col("层级", "level")], dim.value.rankList), Table([Col("子指标", "key"), Col("数值", "value")], dim.value.subValues)], "row", "m")])
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children?: any[], direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- CardHeader(title?: string, subtitle?: string)
- Card(children?: any[], variant?: "card" | "clear" | "sunk", direction?: "row" | "column", gap?: "none" | "xs" | "s" | "m" | "l" | "xl" | "2xl", align?: "start" | "center" | "end" | "stretch" | "baseline", justify?: "start" | "center" | "end" | "between" | "around" | "evenly", wrap?: boolean)
- LineChart(labels: string[], series: any[], variant?: "linear" | "smooth" | "step", xLabel?: string, yLabel?: string)
- Series(category: string, values: number[])
- Table(columns: any[], rows: Record<string, any>[], expandRow?: any)
- Col(title: string, field: string, options?: {cell?: any})
```

---

## unknown-component-datatable (unknown-component, intercepted)

### DSL

```
root = DataTable([regionCol], data.rows)
regionCol = Col("Region", "region")
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "DataTable" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "DataTable" — not found in catalog or builtins (component: DataTable) (stmt=root)

## Component signatures
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## unknown-component-metric-card (unknown-component, intercepted)

### DSL

```
root = Stack([MetricCard("Revenue", data.revenue)])
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "MetricCard" — not found in catalog or builtins stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "MetricCard" — not found in catalog or builtins (component: MetricCard) (stmt=root)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## unknown-component-sparkline (unknown-component, intercepted)

### DSL

```
root = Stack([chartTitle, trend])
chartTitle = TextContent("Trend", "large")
trend = Sparkline(data.values)
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "Sparkline" — not found in catalog or builtins stmt=trend line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "Sparkline" — not found in catalog or builtins (component: Sparkline) (stmt=trend)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## missing-required-textcontent (missing-required, intercepted)

### DSL

```
root = TextContent()
```

### Validator issues (full)

- ERROR [missing-required] missing required field "text" stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [missing-required] missing required field "text" (component: TextContent) (at /text) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## missing-required-table-rows (missing-required, intercepted)

### DSL

```
root = Table([regionCol])
regionCol = Col("Region", "region")
```

### Validator issues (full)

- ERROR [missing-required] missing required field "rows" stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [missing-required] missing required field "rows" (component: Table) (at /rows) (stmt=root)

## Component signatures
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## missing-required-series-data (missing-required, intercepted)

### DSL

```
root = Stack([chart])
labels = data.labels
series = Series("Revenue")
chart = LineChart(labels, [series], "smooth", "Month", "Revenue")
```

### Validator issues (full)

- ERROR [missing-required] missing required field "data" stmt=series line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [missing-required] missing required field "data" (component: Series) (at /data) (stmt=series)

## Component signatures
- Series(name: string, data: any[])
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## null-required-textcontent (null-required, intercepted)

### DSL

```
root = TextContent(null)
```

### Validator issues (full)

- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [null-required] required field "text" cannot be null (component: TextContent) (at /text) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## null-required-table-columns (null-required, intercepted)

### DSL

```
root = Table(null, data.rows)
```

### Validator issues (full)

- ERROR [null-required] required field "columns" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [null-required] required field "columns" cannot be null (component: Table) (at /columns) (stmt=root)

## Component signatures
- Table(columns: any[], rows: any[])
```

---

## null-required-series-data (null-required, intercepted)

### DSL

```
root = Stack([chart])
labels = data.labels
series = Series("Revenue", null)
chart = LineChart(labels, [series], "smooth", "Month", "Revenue")
```

### Validator issues (full)

- ERROR [null-required] required field "data" cannot be null stmt=series line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [null-required] required field "data" cannot be null (component: Series) (at /data) (stmt=series)

## Component signatures
- Series(name: string, data: any[])
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## excess-args-textcontent (excess-args, intercepted)

### DSL

```
root = TextContent("Revenue", "large", "unexpected")
```

### Validator issues (full)

- ERROR [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) (component: TextContent) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## excess-args-stack (excess-args, intercepted)

### DSL

```
root = Stack([], "column", "m", "start", "start", "unexpected")
```

### Validator issues (full)

- ERROR [excess-args] Stack takes 5 arg(s), got 6 (1 excess dropped) stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [excess-args] Stack takes 5 arg(s), got 6 (1 excess dropped) (component: Stack) (stmt=root)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## excess-args-col (excess-args, intercepted)

### DSL

```
root = Table([regionCol], data.rows)
regionCol = Col("Region", "region", {}, "unexpected")
```

### Validator issues (full)

- ERROR [excess-args] Col takes 3 arg(s), got 4 (1 excess dropped) stmt=regionCol line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [excess-args] Col takes 3 arg(s), got 4 (1 excess dropped) (component: Col) (stmt=regionCol)

## Component signatures
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- Table(columns: any[], rows: any[])
```

---

## invalid-prop-col-format (invalid-prop, intercepted)

### DSL

```
root = Table([updatedCol], data.rows)
updatedCol = Col("Updated", "updatedAt", {format: "date"})
```

### Validator issues (full)

- ERROR [invalid-prop] The `format` prop on Col options was removed. Use @FormatDate/@FormatBytes/@FormatNumber/@FormatPercent/@FormatDuration in an expression or @Render(...) instead. stmt=updatedCol line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [invalid-prop] The `format` prop on Col options was removed. Use @FormatDate/@FormatBytes/@FormatNumber/@FormatPercent/@FormatDuration in an expression or @Render(...) instead. (component: Col) (at /options/format) (stmt=updatedCol)

## Component signatures
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- Table(columns: any[], rows: any[])
```

---

## invalid-prop-col-width (invalid-prop, intercepted)

### DSL

```
root = Table([regionCol], data.rows)
regionCol = Col("Region", "region", {width: "120px"})
```

### Validator issues (full)

- ERROR [invalid-prop] Unknown property "width" on Col.options stmt=regionCol line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [invalid-prop] Unknown property "width" on Col.options (component: Col) (at /options/width) (stmt=regionCol)

## Component signatures
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- Table(columns: any[], rows: any[])
```

---

## invalid-prop-tabs-option (invalid-prop, intercepted)

### DSL

```
root = Tabs([
  {value: "overview", label: "Overview", content: [TextContent("Ready")]}
], {unknown: true})
```

### Validator issues (full)

- ERROR [invalid-prop] Unknown property "unknown" on Tabs.options stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [invalid-prop] Unknown property "unknown" on Tabs.options (component: Tabs) (at /options/unknown) (stmt=root)

## Component signatures
- Tabs(items: any[], options?: {style?: Record<string, any>})
- TextContent(text: string, variant?: string)
```

---

## inline-reserved-query-root (inline-reserved, intercepted)

### DSL

```
root = Query("orders", {})
```

### Validator issues (full)

- ERROR [inline-reserved] Query() must be declared as a top-level statement, not used inline as a value stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [inline-reserved] Query() must be declared as a top-level statement, not used inline as a value (component: Query) (stmt=root)
```

---

## inline-reserved-query-child (inline-reserved, intercepted)

### DSL

```
root = Stack([Query("orders", {})])
```

### Validator issues (full)

- ERROR [inline-reserved] Query() must be declared as a top-level statement, not used inline as a value stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [inline-reserved] Query() must be declared as a top-level statement, not used inline as a value (component: Query) (stmt=root)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## inline-reserved-mutation-child (inline-reserved, intercepted)

### DSL

```
root = Card([Mutation("saveOrder", {})], "card", "standard")
```

### Validator issues (full)

- ERROR [inline-reserved] Mutation() must be declared as a top-level statement, not used inline as a value stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [inline-reserved] Mutation() must be declared as a top-level statement, not used inline as a value (component: Mutation) (stmt=root)

## Component signatures
- Card(children: any[], variant?: string, size?: string)
```

---

## syntax-unclosed-component (syntax, intercepted)

### DSL

```
root = TextContent("Missing close"
```

### Validator issues (full)

- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=root line=1:19

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-unclosed-bracket] Unclosed '(' (stmt=root, line 1:19)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## syntax-unclosed-array (syntax, intercepted)

### DSL

```
root = Stack([TextContent("A")
```

### Validator issues (full)

- ERROR [syntax-unclosed-bracket] Unclosed '[' stmt=root line=1:14
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=root line=1:13

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-unclosed-bracket] Unclosed '[' (stmt=root, line 1:14)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=root, line 1:13)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## syntax-missing-assignment (syntax, intercepted)

### DSL

```
root TextContent("A")
```

### Validator issues (full)

- ERROR [syntax-missing-assignment] Statement 'root' is missing '=' stmt=root line=1:1
- ERROR [root-missing] no renderable root component (no statements produced a root component) stmt=null line=-1:-1
  hint: the program must contain at least one renderable component as its entry

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-missing-assignment] Statement 'root' is missing '=' (stmt=root, line 1:1)
- [root-missing] no renderable root component (no statements produced a root component)
  hint: the program must contain at least one renderable component as its entry

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## root-missing-comment (root-missing, intercepted)

### DSL

```
// The model returned commentary but no renderable openui-lang statement.
```

### Validator issues (full)

- ERROR [root-missing] no renderable root component (no statements produced a root component) stmt=null line=-1:-1
  hint: the program must contain at least one renderable component as its entry

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [root-missing] no renderable root component (no statements produced a root component)
  hint: the program must contain at least one renderable component as its entry
```

---

## root-missing-two-comments (root-missing, intercepted)

### DSL

```
// Summary:
// no DSL was emitted.
```

### Validator issues (full)

- ERROR [root-missing] no renderable root component (no statements produced a root component) stmt=null line=-1:-1
  hint: the program must contain at least one renderable component as its entry

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [root-missing] no renderable root component (no statements produced a root component)
  hint: the program must contain at least one renderable component as its entry
```

---

## root-missing-empty-fence (root-missing, intercepted)

### DSL

```
```openui

```
```

### Validator issues (full)

- ERROR [root-missing] no renderable root component (no statements produced a root component) stmt=null line=-1:-1
  hint: the program must contain at least one renderable component as its entry

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [root-missing] no renderable root component (no statements produced a root component)
  hint: the program must contain at least one renderable component as its entry
```

---

## unresolved-ref-missing-panel (unresolved-ref, intercepted)

### DSL

```
root = Stack([missingPanel])
```

### Validator issues (full)

- ERROR [unresolved-ref] unresolved reference "missingPanel" stmt=root line=1:1
  hint: define a statement named "missingPanel" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unresolved-ref] unresolved reference "missingPanel" (stmt=root, line 1:1)
  hint: define a statement named "missingPanel" earlier in the document

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## unresolved-ref-missing-title (unresolved-ref, intercepted)

### DSL

```
root = TextContent(missingTitle)
```

### Validator issues (full)

- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component
- ERROR [unresolved-ref] unresolved reference "missingTitle" stmt=root line=1:1
  hint: define a statement named "missingTitle" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [null-required] required field "text" cannot be null (component: TextContent) (at /text) (stmt=root)
- [unresolved-ref] unresolved reference "missingTitle" (stmt=root, line 1:1)
  hint: define a statement named "missingTitle" earlier in the document

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## unresolved-ref-missing-field (unresolved-ref, intercepted)

### DSL

```
root = Table([regionCol], data.rows)
regionCol = Col("Region", missingField)
```

### Validator issues (full)

- ERROR [null-required] required field "field" cannot be null stmt=regionCol line=-1:-1
- ERROR [unresolved-ref] unresolved reference "missingField" stmt=regionCol line=2:1
  hint: define a statement named "missingField" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [null-required] required field "field" cannot be null (component: Col) (at /field) (stmt=regionCol)
- [unresolved-ref] unresolved reference "missingField" (stmt=regionCol, line 2:1)
  hint: define a statement named "missingField" earlier in the document

## Component signatures
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- Table(columns: any[], rows: any[])
```

---

## valid-single-quote (valid-control, valid)

### DSL

```
root = TextContent('single quotes are legal strings')
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## valid-ternary-nullish (valid-control, valid)

### DSL

```
root = Stack([alarmText, totalText])
alarmText = TextContent(data.criticalAlarms > 0 ? "critical alarms present" : "no critical alarms")
totalText = TextContent(data.total ?? 0)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-fenced-block (valid-control, valid)

### DSL

```
```openui-lang
root = TextContent("fenced output")
```
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## valid-fenced-with-prose (valid-control, valid)

### DSL

```
Here is the UI:

```openui-lang
root = TextContent("fenced output")
```

Hope this helps!
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## valid-ch-bar-axis-labels (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Daily Temperature Highs", "large")
tempSeries = Series("High (C)", data.values)
chart = BarChart(data.labels, [tempSeries], "grouped", "Day", "Temperature (C)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-bar-grouped (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Regional Sales by Product", "large")
labels = data.labels
series = data.series
northValues = @Each(series, "s", s.category == "North" ? s.values : null)
southValues = @Each(series, "s", s.category == "South" ? s.values : null)
northSeries = Series("North", northValues)
southSeries = Series("South", southValues)
chart = BarChart(labels, [northSeries, southSeries], "grouped", "Product", "Units Sold")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-bar-inline-series (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Quarterly Headcount", "large")
chart = BarChart(data.labels, [Series("Headcount", data.values)], "grouped", "Quarter", "People")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
- Series(name: string, data: any[])
```

---

## valid-ch-bar-no-axis (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Weekly Visitors", "large")
visitorSeries = Series("Visitors", data.values)
chart = BarChart(data.labels, [visitorSeries])
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-bar-raw-array (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Static Benchmark Scores", "large")
labels = data.labels
rawSeries = Series("Raw", [10, 20, 30, 40])
chart = BarChart(labels, [rawSeries], "grouped", "Test", "Score")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-bar-stacked (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Monthly Expenses by Category", "large")
labels = data.labels
rentValues = @Each(data.series, "s", s.category == "Rent" ? s.values : null)
utilitiesValues = @Each(data.series, "s", s.category == "Utilities" ? s.values : null)
rentSeries = Series("Rent", rentValues)
utilitiesSeries = Series("Utilities", utilitiesValues)
chart = BarChart(labels, [rentSeries, utilitiesSeries], "stacked", "Month", "Amount ($)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-each-field-map (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Order Totals by Day", "large")
labels = data.labels
totalData = @Each(data.orders, "order", order.total)
totalSeries = Series("Total", totalData)
chart = LineChart(labels, [totalSeries], "raw", "Day", "Total ($)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-line-each-series (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Sensor Readings Over Time", "large")
labels = data.labels
readingData = @Each(data.series, "point", point.value)
readingSeries = Series("Reading", readingData)
chart = LineChart(labels, [readingSeries], "smooth", "Time", "Value")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-line-multi-series (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Revenue vs Cost", "large")
labels = data.labels
revenueValues = @Each(data.series, "s", s.category == "Revenue" ? s.values : null)
costValues = @Each(data.series, "s", s.category == "Cost" ? s.values : null)
revenueSeries = Series("Revenue", revenueValues)
costSeries = Series("Cost", costValues)
chart = LineChart(labels, [revenueSeries, costSeries], "raw", "Quarter", "USD")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-line-smooth-single (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Website Traffic Trend", "large")
labels = data.labels
trafficSeries = Series("Sessions", data.values)
chart = LineChart(labels, [trafficSeries], "smooth", "Week", "Sessions")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-multi-chart-stack (valid-control, valid)

### DSL

```
root = Stack([title, barChart, lineChart])
title = TextContent("Sales Dashboard", "large")
labels = data.labels
barSeries = Series("Units", data.units)
barChart = BarChart(labels, [barSeries], "grouped", "Month", "Units")
lineSeries = Series("Revenue", data.revenue)
lineChart = LineChart(labels, [lineSeries], "smooth", "Month", "Revenue ($)")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Series(name: string, data: any[])
- BarChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
- LineChart(labels: any[], series: any[], variant?: string, xAxis?: string, yAxis?: string)
```

---

## valid-ch-pie-basic (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Market Share by Vendor", "large")
chart = PieChart(data.labels, data.values, "pie")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- PieChart(labels: any[], values: any[], variant?: string)
```

---

## valid-ch-pie-donut (valid-control, valid)

### DSL

```
root = Stack([title, chart])
title = TextContent("Budget Allocation", "large")
labels = data.labels
values = data.values
chart = PieChart(labels, values, "donut")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- PieChart(labels: any[], values: any[], variant?: string)
```

---

## valid-ex-aggregates (valid-control, valid)

### DSL

```
root = Stack([total, average, minimum, maximum, count])
total = TextContent("Sum: " + @Sum(data.values))
average = TextContent("Avg: " + @Avg(data.values))
minimum = TextContent("Min: " + @Min(data.values))
maximum = TextContent("Max: " + @Max(data.values))
count = TextContent("Count: " + @Count(data.values))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-comparisons-logical (valid-control, valid)

### DSL

```
root = Stack([statusText, rangeText, limitText, flagText])
statusText = TextContent(data.score >= 90 && data.active ? "Excellent" : "Needs review")
rangeText = TextContent(data.temp < 0 || data.temp > 100 ? "Out of range" : "Normal")
limitText = TextContent(data.usage <= data.limit ? "within limit" : "over limit")
flagText = TextContent(data.a == data.b ? "match" : (data.a != data.c ? "partial" : "none"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-each-list (valid-control, valid)

### DSL

```
root = List(@Each(data.tasks, "t", TextContent(t.name + " (" + t.status + ")", "small")), "Tasks")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- List(items: any[], title?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-fenced-block (valid-control, valid)

### DSL

```
```openui-lang
root = Stack([title, body])
title = TextContent("Fenced Report", "large")
body = TextContent("Rendered inside a code fence")
```
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-fenced-with-prose (valid-control, valid)

### DSL

```
Here is a dashboard you can use:

```openui-lang
root = Stack([heading, note])
heading = TextContent("Sales Overview", "large")
note = TextContent("Numbers update in real time")
```

Let me know if you want changes.
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-format-dates (valid-control, valid)

### DSL

```
root = Stack([dateText, dateTimeText, timeText])
dateText = TextContent("Date: " + @FormatDate(data.ts, "date"))
dateTimeText = TextContent("DateTime: " + @FormatDate(data.ts, "dateTime"))
timeText = TextContent("Time: " + @FormatDate(data.ts, "time"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-format-num-pct-bytes-dur (valid-control, valid)

### DSL

```
root = Stack([num, pct, bytes, dur])
num = TextContent("Total: " + @FormatNumber(data.amount, 2, "en-US"))
pct = TextContent("Rate: " + @FormatPercent(data.rate))
bytes = TextContent("Size: " + @FormatBytes(data.size))
dur = TextContent("Elapsed: " + @FormatDuration(data.elapsed))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-literals-arithmetic (valid-control, valid)

### DSL

```
// Showcase array literals, object literals, arithmetic, member access, and comments.
root = Stack([summary, itemsTable])
// arithmetic with member access and grouping
summary = TextContent("Net: " + (data.gross - data.fees) / 100)
// array literal of column defs; object literals for column options
itemsTable = Table([nameCol, priceCol], data.items)
nameCol = Col("Name", "name", {sortable: true})
priceCol = Col("Price", "price", {sortable: false})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-ex-nullish-fallbacks (valid-control, valid)

### DSL

```
root = Stack([nameText, countText, combo])
nameText = TextContent(data.user.name ?? "Guest")
countText = TextContent(data.count ?? 0)
combo = TextContent((data.title ?? "Untitled") + " - " + (data.subtitle ?? "no subtitle"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-numeric-builtins (valid-control, valid)

### DSL

```
root = Stack([rounded, absolute, floored, ceiled])
rounded = TextContent("Rounded: " + @Round(data.value))
absolute = TextContent("Abs: " + @Abs(data.delta))
floored = TextContent("Floor: " + @Floor(data.value))
ceiled = TextContent("Ceil: " + @Ceil(data.value))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-object-entries (valid-control, valid)

### DSL

```
root = Stack([keysList, entriesList])
keysList = List(@ObjectKeys(data.config), "Config Keys")
entriesList = List(@Each(@ObjectEntries(data.config), "entry", TextContent(entry.key + ": " + entry.value, "small")), "Config Entries")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- List(items: any[], title?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-render-cell (valid-control, valid)

### DSL

```
root = Stack([userTable])
userTable = Table([nameCol, roleCol], data.users)
nameCol = Col("Name", "name")
roleCol = Col("Role", "role", {cell: @Render("v", "row", TextContent(row.name + " is " + v))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-ex-render-standalone (valid-control, valid)

### DSL

```
root = Stack([greeting])
greeting = @Render("v", TextContent("Value: " + v))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-single-mixed-quotes (valid-control, valid)

### DSL

```
root = Stack([single, mixed])
single = TextContent('all single quoted here')
mixed = TextContent('prefix ' + data.value + " suffix")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-ex-sort-filter (valid-control, valid)

### DSL

```
root = Stack([firstText, lastText, filteredTable])
sortedValues = @Sort(data.values)
firstText = TextContent("First value: " + @First(sortedValues))
lastText = TextContent("Last value: " + @Last(sortedValues))
activeRows = @Filter(data.rows)
filteredTable = Table([nameCol, scoreCol], activeRows)
nameCol = Col("Name", "name")
scoreCol = Col("Score", "score")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-ex-string-concat (valid-control, valid)

### DSL

```
root = TextContent(data.first + " " + data.last + " lives in " + data.city + ", " + data.country)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## valid-ex-switch-mapping (valid-control, valid)

### DSL

```
root = TextContent(@Switch(data.status, {"paid": "Paid", "pending": "Pending", "failed": "Failed"}, "Unknown"))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## valid-ex-ternary-chain (valid-control, valid)

### DSL

```
root = Stack([simple, grade])
simple = TextContent(data.ok ? "yes" : "no")
grade = TextContent(data.score >= 90 ? "A" : data.score >= 80 ? "B" : data.score >= 70 ? "C" : "F")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-card-grid (valid-control, valid)

### DSL

```
root = Stack([cardRow], "column", "l", "start", "start")
cardRow = Stack([usersCard, revenueCard, churnCard], "row", "m", "start", "start")
usersCard = Card([CardHeader("Active Users"), TextContent(@FormatNumber(data.kpi.users, 0, "en-US"), "large")], "card", "standard")
revenueCard = Card([CardHeader("Revenue"), TextContent("$" + @FormatNumber(data.kpi.revenue, 0, "en-US"), "large")], "card", "standard")
churnCard = Card([CardHeader("Churn"), TextContent(@FormatPercent(data.kpi.churn, 1), "large")], "card", "standard")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Card(children: any[], variant?: string, size?: string)
- CardHeader(title: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-card-header-content (valid-control, valid)

### DSL

```
root = Stack([summaryCard])
summaryCard = Card([summaryHeader, summaryBody])
summaryHeader = CardHeader("Monthly Summary")
summaryBody = Stack([TextContent("Total revenue increased 12% versus last month.", "default"), TextContent("New customers: 340", "small")], "column", "m", "start", "start")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Card(children: any[], variant?: string, size?: string)
- CardHeader(title: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-card-variant-size (valid-control, valid)

### DSL

```
root = Stack([kpiCard])
kpiCard = Card([kpiHeader, kpiValue], "highlight", "large")
kpiHeader = CardHeader("Net Promoter Score")
kpiValue = TextContent(@Round(data.metrics.nps, 0) + " NPS", "large")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Card(children: any[], variant?: string, size?: string)
- CardHeader(title: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-list-component-refs (valid-control, valid)

### DSL

```
root = Stack([featureList])
featureList = List([firstItem, secondItem, TextContent("Inline third item", "small")])
firstItem = TextContent("Real-time sync", "default")
secondItem = TextContent("Offline support", "default")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- List(items: any[], title?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-list-each-data (valid-control, valid)

### DSL

```
root = Stack([header, notificationsList], "column", "m", "start", "start")
header = TextContent("Recent Notifications", "large")
notificationsList = List(@Each(data.notifications, "n", TextContent(n.title + " - " + n.time, "small")), "Notifications")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- List(items: any[], title?: string)
```

---

## valid-lc-list-strings (valid-control, valid)

### DSL

```
root = Stack([todoList])
todoList = List(["Review incident report", "Approve deployment", "Update runbook"], "Action Items")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- List(items: any[], title?: string)
```

---

## valid-lc-stack-multi-children (valid-control, valid)

### DSL

```
root = Stack([title, subtitle, body], "column", "m", "start", "start")
title = TextContent("Fleet Overview", "large")
subtitle = TextContent("Live status across all regions", "small")
body = TextContent("All monitored vehicles are reporting normally.", "default")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-stack-nested (valid-control, valid)

### DSL

```
root = Stack([headerRow, contentColumn], "column", "l", "start", "start")
headerRow = Stack([logoText, statusText], "row", "m", "center", "start")
logoText = TextContent("Acme Analytics", "large")
statusText = TextContent("Operational", "small")
contentColumn = Stack([summaryText, detailStack], "column", "s", "start", "start")
detailStack = Stack([TextContent("Uptime 99.98%"), TextContent("Latency 42ms")], "row", "l")
summaryText = TextContent("System summary for the current shift.", "default")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-stack-row-alignment (valid-control, valid)

### DSL

```
root = Stack([leftCol, rightCol], "row", "l", "center", "end")
leftCol = Stack([TextContent("Revenue", "small"), TextContent("$1.2M", "large")], "column", "s", "start", "start")
rightCol = Stack([TextContent("Orders", "small"), TextContent("8,431", "large")], "column", "s", "end", "start")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-stack-single-child (valid-control, valid)

### DSL

```
root = Stack([greeting])
greeting = TextContent("Welcome back to the operations dashboard.", "large")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-tabs-plain (valid-control, valid)

### DSL

```
root = Stack([dashboardTabs])
dashboardTabs = Tabs([overviewTab, detailsTab])
overviewTab = Card([CardHeader("Overview"), TextContent("High-level metrics for the account.", "default")])
detailsTab = Card([CardHeader("Details"), TextContent("Line-by-line breakdown of usage.", "default")])
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Tabs(items: any[], options?: {style?: Record<string, any>})
- Card(children: any[], variant?: string, size?: string)
- CardHeader(title: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-tabs-styled (valid-control, valid)

### DSL

```
root = Stack([reportTabs])
reportTabs = Tabs([salesTab, opsTab], {style: {orientation: "horizontal", size: "large"}})
salesTab = Stack([TextContent("Sales", "large"), TextContent("Pipeline value $4.2M", "default")], "column", "m", "start", "start")
opsTab = Stack([TextContent("Operations", "large"), List(["Servers healthy", "Queues drained"], "Status")], "column", "m", "start", "start")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Tabs(items: any[], options?: {style?: Record<string, any>})
- TextContent(text: string, variant?: string)
- List(items: any[], title?: string)
```

---

## valid-lc-textcontent-expressions (valid-control, valid)

### DSL

```
root = Stack([statusText, fallbackText, countText], "column", "m", "start", "start")
statusText = TextContent(data.system.healthy ? "All systems healthy" : "Degraded performance detected", "large")
fallbackText = TextContent(data.user.displayName ?? "Anonymous user", "default")
countText = TextContent("Active alerts: " + @Count(data.alerts), "small")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-lc-textcontent-variants (valid-control, valid)

### DSL

```
root = Stack([bigText, normalText, smallText, concatText], "column", "m", "start", "start")
bigText = TextContent("Quarterly Report", "large")
normalText = TextContent("Prepared for the leadership team.", "default")
smallText = TextContent("Confidential", "small")
concatText = TextContent("Region: " + data.report.region + " (" + data.report.code + ")", "default")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## valid-tb-basic-plain (valid-control, valid)

### DSL

```
root = Stack([productTable])
productTable = Table([skuCol, nameCol, categoryCol], data.products)
skuCol = Col("SKU", "sku")
nameCol = Col("Name", "name")
categoryCol = Col("Category", "category")
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-tb-cell-single-binder (valid-control, valid)

### DSL

```
root = Stack([priceTable])
priceTable = Table([itemCol, priceCol], data.catalog)
itemCol = Col("Item", "item")
priceCol = Col("Price", "price", {cell: @Render("v", TextContent(v + " USD"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-cell-two-binders (valid-control, valid)

### DSL

```
root = Stack([invoiceTable])
invoiceTable = Table([idCol, amountCol], data.invoices)
idCol = Col("Invoice", "id")
amountCol = Col("Amount", "amount", {cell: @Render("v", "row", TextContent(row.id + ": " + v))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-format-bytes-percent-duration (valid-control, valid)

### DSL

```
root = Stack([fileTable])
fileTable = Table([nameCol, sizeCol, usageCol, elapsedCol], data.files)
nameCol = Col("File", "name")
sizeCol = Col("Size", "size", {cell: @Render("v", TextContent(@FormatBytes(v)))})
usageCol = Col("Usage", "usage", {cell: @Render("v", TextContent(@FormatPercent(v)))})
elapsedCol = Col("Elapsed", "elapsed", {cell: @Render("v", TextContent(@FormatDuration(v)))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-format-number-date (valid-control, valid)

### DSL

```
root = Stack([txnTable])
txnTable = Table([refCol, amountCol, createdCol, settledCol], data.transactions)
refCol = Col("Reference", "ref")
amountCol = Col("Amount", "amount", {cell: @Render("v", TextContent(@FormatNumber(v, 2, "en-US")))})
createdCol = Col("Created", "createdAt", {cell: @Render("v", TextContent(@FormatDate(v, "date")))})
settledCol = Col("Settled", "settledAt", {cell: @Render("v", TextContent(@FormatDate(v, "dateTime")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-inline-columns (valid-control, valid)

### DSL

```
root = Table([Col("City", "city"), Col("Population", "population", {sortable: true, cell: @Render("v", TextContent(@FormatNumber(v, 0, "en-US")))}), Col("Country", "country")], data.cities)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-many-columns (valid-control, valid)

### DSL

```
root = Stack([employeeTable])
employeeTable = Table([idCol, nameCol, deptCol, salaryCol, joinedCol, ratioCol], data.employees)
idCol = Col("ID", "id")
nameCol = Col("Name", "name", {sortable: true})
deptCol = Col("Department", "department")
salaryCol = Col("Salary", "salary", {sortable: true, cell: @Render("v", TextContent(@FormatNumber(v, 0, "en-US") + " USD"))})
joinedCol = Col("Joined", "joinedAt", {cell: @Render("v", TextContent(@FormatDate(v, "date")))})
ratioCol = Col("Bonus", "bonusRatio", {cell: @Render("v", TextContent(@FormatPercent(v)))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-root-direct-table (valid-control, valid)

### DSL

```
root = metricsTable
metricsTable = Table([metricCol, valueCol, changeCol], data.metrics)
metricCol = Col("Metric", "metric")
valueCol = Col("Value", "value", {cell: @Render("v", TextContent(@FormatNumber(v, 1, "en-US")))})
changeCol = Col("Change", "change", {cell: @Render("v", "row", TextContent(row.value > 0 ? "Up" : "Down"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-sortable-cols (valid-control, valid)

### DSL

```
root = Stack([usersTable])
usersTable = Table([nameCol, emailCol, ageCol], data.users)
nameCol = Col("Name", "name", {sortable: true})
emailCol = Col("Email", "email")
ageCol = Col("Age", "age", {sortable: true})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-tb-stack-title (valid-control, valid)

### DSL

```
root = Stack([title, subtitle, revenueTable], "vertical", 12)
title = TextContent("Quarterly Revenue", "large")
subtitle = TextContent("All figures in USD", "small")
revenueTable = Table([quarterCol, revenueCol], data.quarters)
quarterCol = Col("Quarter", "quarter")
revenueCol = Col("Revenue", "revenue", {cell: @Render("v", TextContent(@FormatNumber(v, 0, "en-US")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## valid-tb-switch-status (valid-control, valid)

### DSL

```
root = Stack([ticketTable])
ticketTable = Table([idCol, statusCol], data.tickets)
idCol = Col("Ticket", "id")
statusCol = Col("Status", "status", {cell: @Render("v", TextContent(@Switch(v, {"open": "Open", "closed": "Closed", "wip": "In Progress"}, "Unknown")))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## valid-tb-ternary-threshold (valid-control, valid)

### DSL

```
root = Stack([scoreTable])
scoreTable = Table([playerCol, scoreCol], data.scores)
playerCol = Col("Player", "player")
scoreCol = Col("Score", "score", {cell: @Render("v", TextContent(v > 100 ? "High" : "Low"))})
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
- TextContent(text: string, variant?: string)
```

---

## js-method-call-tostring (js-method-call, intercepted)

### DSL

```
root = Stack([kpiLabel, kpiValue])
kpiLabel = TextContent("Total devices", "small")
kpiValue = TextContent(data.totalDevices.toString(), "large-heavy")
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=kpiValue line=3:51
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=kpiValue line=3:50
- ERROR [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) stmt=kpiValue line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=kpiValue, line 3:51)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=kpiValue, line 3:50)

## Component signatures
- TextContent(text: string, variant?: string)
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## js-method-call-tostring-ternary (js-method-call, intercepted)

### DSL

```
root = Stack([delta])
delta = TextContent(data.totalAlarms.delta < 0 ? "down" : "up" + Math.abs(data.totalAlarms.delta).toString(), "small")
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=delta line=2:108
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=delta line=2:107
- ERROR [excess-args] TextContent takes 2 arg(s), got 4 (2 excess dropped) stmt=delta line=-1:-1
- ERROR [unresolved-ref] unresolved reference "Math" stmt=delta line=2:1
  hint: "Math" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=delta, line 2:108)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=delta, line 2:107)
- [unresolved-ref] unresolved reference "Math" (stmt=delta, line 2:1)
  hint: "Math" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

## Component signatures
- TextContent(text: string, variant?: string)
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## js-method-call-map-arrow (js-method-call, intercepted)

### DSL

```
root = Stack([totalIn])
totalIn = TextContent(@FormatNumber(@Sum(data.devices.map(d => d.inTraffic)), 1))
```

### Validator issues (full)

- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=totalIn line=2:58
- ERROR [syntax-unexpected-token] Unexpected token EQUALS stmt=totalIn line=2:61
- ERROR [unresolved-ref] unresolved reference "d" stmt=totalIn line=2:1
  hint: define a statement named "d" earlier in the document
- ERROR [unresolved-ref] unresolved reference "d" stmt=totalIn line=2:1
  hint: define a statement named "d" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-unclosed-bracket] Unclosed '(' (stmt=totalIn, line 2:58)
- [syntax-unexpected-token] Unexpected token EQUALS (stmt=totalIn, line 2:61)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
- TextContent(text: string, variant?: string)
```

---

## js-global-math-abs (js-global, intercepted)

### DSL

```
root = TextContent("delta " + Math.abs(data.delta))
```

### Validator issues (full)

- ERROR [unresolved-ref] unresolved reference "Math" stmt=root line=1:1
  hint: "Math" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unresolved-ref] unresolved reference "Math" (stmt=root, line 1:1)
  hint: "Math" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## js-global-new-date (js-global, intercepted)

### DSL

```
root = TextContent(new Date().toISOString())
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=root line=1:43
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=root line=1:19
- ERROR [unknown-component] Unknown component "Date" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) stmt=root line=-1:-1
- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component
- ERROR [unresolved-ref] unresolved reference "new" stmt=root line=1:1
  hint: "new" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=root, line 1:43)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=root, line 1:19)
- [unknown-component] Unknown component "Date" — not found in catalog or builtins (component: Date) (stmt=root)
- [unresolved-ref] unresolved reference "new" (stmt=root, line 1:1)
  hint: "new" is a JavaScript global — JS globals and methods are not available in openui-lang; use builtins like @Abs, @Round, @FormatNumber, @FormatDate instead

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## js-global-string-conversion (js-global, intercepted)

### DSL

```
root = TextContent(String(data.total))
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "String" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "String" — not found in catalog or builtins (component: String) (stmt=root)
- [null-required] required field "text" cannot be null (component: TextContent) (at /text) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## js-syntax-arrow-helper (js-syntax-construct, intercepted)

### DSL

```
cardHeader = (title) => CardHeader(title)
root = Stack([cardHeader("CPU usage")])
```

### Validator issues (full)

- ERROR [unresolved-ref] unresolved reference "title" stmt=cardHeader line=1:1
  hint: define a statement named "title" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unresolved-ref] unresolved reference "title" (stmt=cardHeader, line 1:1)
  hint: define a statement named "title" earlier in the document

## Component signatures
- CardHeader(title: string)
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## js-syntax-template-string (js-syntax-construct, intercepted)

### DSL

```
root = TextContent(`Total: ${data.total}`)
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token COLON stmt=root line=1:26
- ERROR [syntax-unexpected-token] Unexpected token DOT stmt=root line=1:34
- ERROR [syntax-unexpected-token] Unexpected token R_BRACE stmt=root line=1:40
- ERROR [syntax-unclosed-bracket] Unclosed '{' stmt=root line=1:29
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=root line=1:19
- ERROR [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) stmt=root line=-1:-1
- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component
- ERROR [unresolved-ref] unresolved reference "Total" stmt=root line=1:1
  hint: define a statement named "Total" earlier in the document

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Named component arguments are not supported; convert `foo: value` inside component calls to positional arguments, but keep object-literal keys when an object prop is required.
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token COLON (stmt=root, line 1:26)
- [syntax-unexpected-token] Unexpected token DOT (stmt=root, line 1:34)
- [syntax-unexpected-token] Unexpected token R_BRACE (stmt=root, line 1:40)
- [syntax-unclosed-bracket] Unclosed '{' (stmt=root, line 1:29)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=root, line 1:19)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## js-syntax-spread (js-syntax-construct, intercepted)

### DSL

```
root = Stack([...data.items])
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token DOT stmt=root line=1:15

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token DOT (stmt=root, line 1:15)

## Component signatures
- Stack(children: any[], direction?: string, gap?: string, align?: string, justify?: string)
```

---

## builtin-misuse-bare-format-number (builtin-misuse, intercepted)

### DSL

```
root = TextContent(FormatNumber(data.total, 1))
```

### Validator issues (full)

- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=root line=1:32
- ERROR [excess-args] TextContent takes 2 arg(s), got 3 (1 excess dropped) stmt=root line=-1:-1
- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component
- ERROR [unresolved-ref] unresolved reference "FormatNumber" stmt=root line=1:1
  hint: did you mean "@FormatNumber"? openui-lang builtins must be called with a leading '@'

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-unclosed-bracket] Unclosed '(' (stmt=root, line 1:32)
- [unresolved-ref] unresolved reference "FormatNumber" (stmt=root, line 1:1)
  hint: did you mean "@FormatNumber"? openui-lang builtins must be called with a leading '@'

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## builtin-misuse-invented-div-mul (builtin-misuse, intercepted)

### DSL

```
root = TextContent(@FormatNumber(@Mul(@Div(data.activeDevices, data.totalDevices), 100), 1))
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "Mul" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [unknown-component] Unknown component "Div" — not found in catalog or builtins stmt=root line=-1:-1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "Mul" — not found in catalog or builtins (component: Mul) (stmt=root)
- [unknown-component] Unknown component "Div" — not found in catalog or builtins (component: Div) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## builtin-misuse-invented-clamp (builtin-misuse, intercepted)

### DSL

```
root = TextContent(@Clamp(data.total, 0, 1))
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "Clamp" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [null-required] required field "text" cannot be null stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "Clamp" — not found in catalog or builtins (component: Clamp) (stmt=root)
- [null-required] required field "text" cannot be null (component: TextContent) (at /text) (stmt=root)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## unknown-component-minichart (unknown-component, intercepted)

### DSL

```
root = MiniChart("line", [data.avgCpuUtil], 96)
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins (component: MiniChart) (stmt=root)
```

---

## unknown-component-legacy-vlayout (unknown-component, intercepted)

### DSL

```
root = VLayout([Text("Network summary", "large")])
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "VLayout" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "VLayout" — not found in catalog or builtins (component: VLayout) (stmt=root)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=root)
```

---

## unknown-component-descriptions (unknown-component, intercepted)

### DSL

```
root = Descriptions([DescField("Device name", data.neName)])
```

### Validator issues (full)

- ERROR [unknown-component] Unknown component "Descriptions" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [unknown-component] Unknown component "DescField" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- [unknown-component] Unknown component "Descriptions" — not found in catalog or builtins (component: Descriptions) (stmt=root)
- [unknown-component] Unknown component "DescField" — not found in catalog or builtins (component: DescField) (stmt=root)
```

---

## prose-chatter-intro (prose-chatter, intercepted)

### DSL

```
Sure! Here is a simple layout:
root = TextContent("hi")
```

### Validator issues (full)

- ERROR [syntax-missing-assignment] Statement 'Sure' is missing '=' stmt=Sure line=1:1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-missing-assignment] Statement 'Sure' is missing '=' (stmt=Sure, line 1:1)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## prose-chatter-trailing (prose-chatter, intercepted)

### DSL

```
root = TextContent("hi")
Hope this helps!
```

### Validator issues (full)

- ERROR [syntax-missing-assignment] Statement 'Hope' is missing '=' stmt=Hope line=2:1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-missing-assignment] Statement 'Hope' is missing '=' (stmt=Hope, line 2:1)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## prose-chatter-bullet (prose-chatter, intercepted)

### DSL

```
root = TextContent("hi")
- root shows a greeting
```

### Validator issues (full)

- ERROR [syntax-invalid-statement] Line does not start a valid statement stmt=MINUS line=2:1

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.

## Validation issues to fix
- [syntax-invalid-statement] Line does not start a valid statement (stmt=MINUS, line 2:1)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## known-miss-envelope (known-miss, missed)

### DSL

```
root = Table([Col("Name", "name")], data.data.users)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## known-miss-field-typo (known-miss, missed)

### DSL

```
root = Table([Col("Name", "nmae")], data.userz)
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- Table(columns: any[], rows: any[])
- Col(title: string, field: string, options?: {sortable?: boolean, cell?: any})
```

---

## known-miss-method-1arg (known-miss, missed)

### DSL

```
root = TextContent(data.value.toFixed(1))
```

### Validator issues (full)

(none)

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Validation issues to fix
- (no structured issues supplied)

## Component signatures
- TextContent(text: string, variant?: string)
```

---

## real-file-aggregated-kpi (real-file, intercepted)

### DSL

```
root = VLayout([
  summaryCard,
  statusGrid,
  trendSection
])

summaryCard = Card([cardTitle, kpiRows], "card", "standard")
cardTitle = Text("网络整体运行状况摘要", "large")
kpiRows = VLayout([
  kpiItem1,
  kpiItem2,
  kpiItem3,
  kpiItem4
], "m", "start", "between")

kpiItem1 = HLayout([
  Text("总设备数", "small"),
  Text(data.totalDevices.toString(), "large-heavy")
], "s", "center", "end")

kpiItem2 = HLayout([
  Text("活跃设备数", "small"),
  Text(data.activeDevices.toString(), "large-heavy")
], "s", "center", "end")

kpiItem3 = HLayout([
  Text("告警总数", "small"),
  Text(data.totalAlarms.toString(), "large-heavy")
], "s", "center", "end")

kpiItem4 = HLayout([
  Text("关键告警数", "small"),
  Text(data.criticalAlarms.toString(), "large-heavy")
], "s", "center", "end")

statusGrid = VLayout([
  deviceStatusRow,
  alarmStatusRow,
  performanceStatusRow
], "l", "start", "between")

deviceStatusRow = HLayout([
  Text("设备状态", "small"),
  Tag(
    data.activeDevices + "/" + data.totalDevices,
    @Switch(
      @Round(@Div(data.activeDevices, data.totalDevices), 2),
      {
        "1": "success",
        "0.9": "warning",
        "0.8": "warning",
        "0.7": "danger"
      },
      "danger"
    )
  )
], "m", "center", "start")

alarmStatusRow = HLayout([
  Text("告警状态", "small"),
  Tag(
    data.criticalAlarms > 0 ? "有关键告警" : "无关键告警",
    data.criticalAlarms > 0 ? "danger" : "success"
  )
], "m", "center", "start")

performanceStatusRow = HLayout([
  Text("可用率", "small"),
  Tag(
    @FormatPercent(data.availabilityRate, 2) + " (" + @FormatNumber(@Mul(@Div(data.activeDevices, data.totalDevices), 100), 1) + "%)",
    @Switch(
      data.availabilityRate,
      {
        "0.95": "danger",
        "0.9": "warning",
        "0.8": "warning",
        "0.7": "danger"
      },
      "success"
    )
  )
], "m", "center", "start")

trendSection = VLayout([
  Text("性能趋势", "large"),
  cpuTrendChart,
  memTrendChart
], "l", "start", "start")

cpuTrendChart = MiniChart("line", [data.avgCpuUtil], 96)
memTrendChart = MiniChart("line", [data.avgMemUtil], 96)
```

### Validator issues (full)

- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=kpiItem1 line=18:35
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=kpiItem1 line=18:34
- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=kpiItem2 line=23:36
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=kpiItem2 line=23:35
- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=kpiItem3 line=28:34
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=kpiItem3 line=28:33
- ERROR [syntax-unexpected-token] Unexpected token R_PAREN stmt=kpiItem4 line=33:37
- ERROR [syntax-unclosed-bracket] Unclosed '(' stmt=kpiItem4 line=33:36
- ERROR [unknown-component] Unknown component "VLayout" — not found in catalog or builtins stmt=root line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=cardTitle line=-1:-1
- ERROR [unknown-component] Unknown component "VLayout" — not found in catalog or builtins stmt=kpiRows line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=kpiItem1 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem1 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem1 line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=kpiItem2 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem2 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem2 line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=kpiItem3 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem3 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem3 line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=kpiItem4 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem4 line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=kpiItem4 line=-1:-1
- ERROR [unknown-component] Unknown component "VLayout" — not found in catalog or builtins stmt=statusGrid line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=deviceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=deviceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Tag" — not found in catalog or builtins stmt=deviceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Div" — not found in catalog or builtins stmt=deviceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=alarmStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=alarmStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Tag" — not found in catalog or builtins stmt=alarmStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "HLayout" — not found in catalog or builtins stmt=performanceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=performanceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Tag" — not found in catalog or builtins stmt=performanceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Mul" — not found in catalog or builtins stmt=performanceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "Div" — not found in catalog or builtins stmt=performanceStatusRow line=-1:-1
- ERROR [unknown-component] Unknown component "VLayout" — not found in catalog or builtins stmt=trendSection line=-1:-1
- ERROR [unknown-component] Unknown component "Text" — not found in catalog or builtins stmt=trendSection line=-1:-1
- ERROR [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins stmt=cpuTrendChart line=-1:-1
- ERROR [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins stmt=memTrendChart line=-1:-1
- ERROR [root-not-renderable] root statement "root" did not resolve to a renderable component stmt=root line=-1:-1
  hint: the entry statement must be a valid catalog component

### Reask prompt (system, post-suppression)

```
[REPAIR:FULL]
You previously produced an openui-lang document that FAILED validation. Regenerate a CORRECTED, COMPLETE openui-lang document that fixes every issue below. Return ONLY the corrected openui-lang inside a single ```openui fenced block. Do not explain.

## Compact openui-lang rules for repair
1. Output only the requested openui-lang repair; do not add explanations.
2. One statement has the form `name = Expression`; keep one complete assignment per statement.
3. `root` must ultimately be a renderable component, usually `root = Stack([...])`.
4. Arguments are POSITIONAL. Write `Stack(children, "row", "l")`, not named component args.
5. Object literals are allowed only as expression values or object props; keep `{}`, `[]`, and `()` balanced.
6. Builtins must start with `@`, for example `@Each(array, "item", expr)` and `@Render("v", expr)`.
7. Binder names are string literals and are only in scope inside that builtin template.
8. JavaScript globals and methods are unavailable; use openui-lang builtins instead.

## Syntax repair hints
- Regenerate the whole invalid statement using the signatures below instead of patching a token in place.
- Close every `(`, `[`, and `{` opened by the invalid statement before starting another statement.

## Validation issues to fix
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=kpiItem1, line 18:35)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=kpiItem1, line 18:34)
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=kpiItem2, line 23:36)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=kpiItem2, line 23:35)
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=kpiItem3, line 28:34)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=kpiItem3, line 28:33)
- [syntax-unexpected-token] Unexpected token R_PAREN (stmt=kpiItem4, line 33:37)
- [syntax-unclosed-bracket] Unclosed '(' (stmt=kpiItem4, line 33:36)
- [unknown-component] Unknown component "VLayout" — not found in catalog or builtins (component: VLayout) (stmt=root)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=cardTitle)
- [unknown-component] Unknown component "VLayout" — not found in catalog or builtins (component: VLayout) (stmt=kpiRows)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=kpiItem1)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem1)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem1)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=kpiItem2)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem2)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem2)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=kpiItem3)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem3)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem3)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=kpiItem4)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem4)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=kpiItem4)
- [unknown-component] Unknown component "VLayout" — not found in catalog or builtins (component: VLayout) (stmt=statusGrid)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=deviceStatusRow)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=deviceStatusRow)
- [unknown-component] Unknown component "Tag" — not found in catalog or builtins (component: Tag) (stmt=deviceStatusRow)
- [unknown-component] Unknown component "Div" — not found in catalog or builtins (component: Div) (stmt=deviceStatusRow)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=alarmStatusRow)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=alarmStatusRow)
- [unknown-component] Unknown component "Tag" — not found in catalog or builtins (component: Tag) (stmt=alarmStatusRow)
- [unknown-component] Unknown component "HLayout" — not found in catalog or builtins (component: HLayout) (stmt=performanceStatusRow)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=performanceStatusRow)
- [unknown-component] Unknown component "Tag" — not found in catalog or builtins (component: Tag) (stmt=performanceStatusRow)
- [unknown-component] Unknown component "Mul" — not found in catalog or builtins (component: Mul) (stmt=performanceStatusRow)
- [unknown-component] Unknown component "Div" — not found in catalog or builtins (component: Div) (stmt=performanceStatusRow)
- [unknown-component] Unknown component "VLayout" — not found in catalog or builtins (component: VLayout) (stmt=trendSection)
- [unknown-component] Unknown component "Text" — not found in catalog or builtins (component: Text) (stmt=trendSection)
- [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins (component: MiniChart) (stmt=cpuTrendChart)
- [unknown-component] Unknown component "MiniChart" — not found in catalog or builtins (component: MiniChart) (stmt=memTrendChart)

## Component signatures
- Card(children: any[], variant?: string, size?: string)
```
